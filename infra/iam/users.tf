# Neutralized for verification (attacker-controlled PR content).
# Original referenced google_project_iam_member, which would force the google
# provider and abort plan at provider configuration (no GCP creds on runner).
